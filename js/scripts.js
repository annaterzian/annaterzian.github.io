{
	/* <script>
    const konamiCode = [38, 38, 40, 40, 37, 39, 37, 39, 66, 65]; // Konami code sequence
    let konamiIndex = 0;

    // Listen for keydown events
    document.addEventListener('keydown', function(e) {
        if (e.keyCode === konamiCode[konamiIndex]) {
            konamiIndex++;
            // If the full Konami code is entered correctly
            if (konamiIndex === konamiCode.length) {
                alert('Konami code activated! Redirecting...');
                window.location.href = '/easter-egg.html'; // Redirect to the hidden Twitch page
            }
        } else {
            konamiIndex = 0; // Reset if incorrect sequence
        }
    });
</script> */
}
